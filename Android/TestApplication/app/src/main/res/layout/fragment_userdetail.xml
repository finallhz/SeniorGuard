<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F0F3F6"
    >

    <!-- 全圆角，用户昵称和用户头像-->
    <LinearLayout style="@style/wrap_layout">
        <LinearLayout style="@style/single_layout">
            <ImageView  android:id="@+id/ivhead" style="@style/img_arrow" android:src="@drawable/user_icon"/>
            <TextView
                android:id="@+id/userlogin"
                style="@style/usertext"
                android:layout_width="match_parent"
                android:background="@color/white"
                android:text="请您登录"
                android:layout_marginLeft="10dp"
                android:layout_height="80dp"
                android:gravity="center_vertical"
                android:textSize="18sp"
                 />

        </LinearLayout>

    </LinearLayout>

    <LinearLayout style="@style/wrap_layout" >
        <!-- 上圆角-->
        <LinearLayout style="@style/top_layout" android:id="@+id/ll1">
            <TextView style="@style/usertext" android:text="个性签名" />
            <ImageView style="@style/img_arrow"/>
        </LinearLayout>

        <!-- 分割线 -->
        <View style="@style/bg_line"/>
        <!-- 无圆角-->
        <LinearLayout style="@style/mid_layout" android:id="@+id/ll2">
            <TextView style="@style/usertext" android:text="我的资料"  />
            <ImageView style="@style/img_arrow"/>
        </LinearLayout>
        <View style="@style/bg_line"/>
        <!-- 下圆角-->
        <LinearLayout style="@style/bottom_layout" android:id="@+id/ll3">
            <TextView style="@style/usertext" android:text="消息通知" />
            <ImageView style="@style/img_arrow"/>
        </LinearLayout>

    </LinearLayout>

    <!-- 全圆角-->
    <LinearLayout style="@style/wrap_layout">
        <LinearLayout style="@style/single_layout" android:id="@+id/ll4">
            <TextView style="@style/usertext" android:text="紧急联系人" />
            <ImageView style="@style/img_arrow"/>
        </LinearLayout>

    </LinearLayout>

    <!-- 全圆角-->
    <LinearLayout style="@style/wrap_layout">
        <LinearLayout style="@style/single_layout" android:id="@+id/ll5">
            <TextView style="@style/usertext" android:text="辅助功能"/>
            <ImageView style="@style/img_arrow"/>
        </LinearLayout>

    </LinearLayout>
</LinearLayout>
